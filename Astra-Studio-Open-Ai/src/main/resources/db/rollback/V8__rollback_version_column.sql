-- ============================================================================
-- V8 ROLLBACK: 移除乐观锁 version 列
-- ============================================================================
-- 执行条件: V8__add_version_column.sql 部署后需要回退时
-- 执行顺序: 必须在应用停机维护期间执行，确保无活跃事务

-- 1. 删除乐观锁版本字段
ALTER TABLE conversations DROP COLUMN IF EXISTS version;

-- 注意:
-- - 回滚后 @Version 注解将失效，乐观锁机制不可用
-- - 建议在应用配置中同步禁用乐观锁相关功能
-- - 回滚前先备份数据库
